using System;

namespace Operadores
{
    class Program
    {
        static void Main(string[] args)
        {
            // operadores aritméticos (+ - / * %)
            // csharp tem o pow somente utilizando a class Math
            Console.WriteLine(Math.Pow(4, 2));
            // operadores relacionais (> < != ==)
            // operadores lógicos (|| &)
        }
    }
}
